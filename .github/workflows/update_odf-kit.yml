on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - run: ./src/odf-kit/build.sh
      - name: Commit and push changes
        run: |
          git config --global user.name "Abdulrahman"
          git config --global user.email "176532798+abdulrahman-103@users.noreply.github.com"
          git add .
          git commit -m "Update odf-kit" || echo "No changes to commit"
          git push
